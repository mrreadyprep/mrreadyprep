# Pinned to exact versions (rather than left open) so a fresh `pip install` on every Render
# redeploy can't silently pull a newer release with different default behavior -- or, in a worst
# case, a compromised release on PyPI -- without a deliberate version bump reviewed here first.
# Bump deliberately: change the version, redeploy to a staging/preview env if possible, confirm
# nothing broke, then ship.
uvicorn[standard]==0.51.0
fastapi==0.140.0
edge-tts==7.2.8
imageio-ffmpeg==0.6.0
bcrypt==3.2.0
PyJWT==2.3.0
pydantic[email]==2.13.4
google-auth==2.56.2
requests==2.34.2
httpx==0.28.1
psycopg2-binary==2.9.12
